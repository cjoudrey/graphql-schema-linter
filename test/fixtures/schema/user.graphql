interface Node {
  id: ID!
}

interface Entity {
  id: ID!
}

type User implements Node & Entity {
  id: ID!
  username: String!
  email: String!
}

type OldUser implements Node, Entity {
  id: ID!
}

extend type Query {
  viewer: User!
}
